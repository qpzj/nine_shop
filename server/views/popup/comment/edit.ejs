<!DOCTYPE html>
<html class="x-admin-sm">
<%- include('../../common/head'); %>

<body>
    <div class="layui-fluid">
        <div class="layui-row">
            <form class="layui-form" action="toEdit" method="post">
                            <input type="hidden" name="id" value="<%=comment.id%>">
                <div class="layui-form-item">
                    <label for="L_nick_name" class="layui-form-label">
                        评价内容</label>
                    <div class="layui-input-inline">
                       
                            <textarea name="content" value="<%=comment.content%>" required lay-verify="required" placeholder="请输入" class="layui-textarea"><%=comment.content%></textarea>
                </div></div>
                <div class="layui-form-item">
                    <label for="role" class="layui-form-label">评价星星：</label>
                    <div class="layui-input-inline">
                      <select name="stars" lay-verify="required" id="role" >
                          <%for(var i=0;i<=5;i++){%>
                            <option  value="<%=i%>" <%if(comment.stars==i){%>selected<%}%>> <%=i%></option>

                            <%}%>
                       
                       
                      </select>     
                      
                </div>
              </div>
                 
                <div class="layui-form-item">
                    <label for="L_repass" class="layui-form-label"></label>
                    <button class="layui-btn" lay-filter="updataInfo" lay-submit="">修改</button></div>
            </form>
        </div>
    </div>
    
<script>
        layui.use('form', function() {
        var form = layui.form; //只有执行了这一步，部分表单元素才会自动修饰成功
     
        //……
     
       

        //但是，如果你的HTML是动态生成的，自动渲染就会失效
        //因此你需要在相应的地方，执行下述方法来手动渲染，跟这类似的还有 element.init();
        form.render();
        });
    </script>
</body>

</html>